version: '3.8'

services:
    web:
        build: .
        command: python /code/manage.py runserver 0.0.0.0:8000
        # command: gunicorn config.wsgi -b 0.0.0.0:8000
        volumes:
            - .:/code
        ports:
            - 8000:8000
        depends_on:
            - db
        environment:
            - "DJANGO_SECRET_KEY=)*_s#exg*#w+#-xt=vu8b010%%a&p@4edwyj0=(nqq90b9a8*n"
            - "DJANGO_DEBUG=True"
            - "DJANGO_SECURE_SSL_REDIRECT=False"
            - "DJANGO_SECURE_HSTS_SECONDS=0"
            - "DJANGO_SECURE_HSTS_INCLUDE_SUBDOMAINS=False"
            - "DJANGO_SECURE_HSTS_PRELOAD=False"
            - "DJANGO_SESSION_COOKIE_SECURE=False"
            - "DJANGO_CSRF_COOKIE_SECURE=False"
            - "USE_S3=True"
            - "AWS_ACCESS_KEY_ID=AKIARCAQHQ5ILFRGVVP5"
            - "AWS_SECRET_ACCESS_KEY=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"
            - "AWS_STORAGE_BUCKET_NAME=samson-static-1319"
            - "EMAIL_HOST_USER=jamie.appleyard@samsonsecurity.co.uk"
            - "EMAIL_HOST_PASSWORD=Samson123!"
            - "DEFAULT_FROM_EMAIL=jamie.appleyard@samsonsecurity.co.uk"
    db:
        image: postgres:11
        volumes:
            - postgres_data:/var/lib/postgresql/data/
        environment:
            - "POSTGRES_HOST_AUTH_METHOD=trust"
        
volumes:
    postgres_data: